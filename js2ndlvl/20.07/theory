DATA
 -storage
    vars, DOM ---- минусы( до след. перезагрузки страницы )

    persistent долгосрочность
    - cookie (old)самый старый механизм
      - объём данных ограничен 2kb-4kb
      - старый формат -> неудобная работа с js
      - идентификация клиента(язык)

    - localStorage - запоминает данные потенциально навсегда
    - sessionStorage - только до закрытия окна
      - объём данных 50-100мб
      - только js (браузер) имеет доступ к данным
      - функционал более продвинутый
    - Cache API

    // отличаются целью, моментом появления,
 -process
 -exchange
